# Roadmap

- **Web-based**: browser UI for generating Blueprint logic and exporting snippets.
- **Standalone**: desktop app that runs outside the editor while targeting your project assets.
- **UPlugin**: packaged Unreal Engine plugin for drop-in installation.
